name: seim_canary
description: "A new Flutter project."
publish_to: 'none'
version: 0.1.0

environment:
  sdk: ^3.6.1

dependencies:
  uuid: ^3.0.4
  bcrypt: 1.1.3
  cloud_firestore: ^5.6.5
  crypto: ^3.0.6
  firebase_core: ^3.12.1
  flutter:
    sdk: flutter

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^5.0.0

flutter:
  uses-material-design: true
